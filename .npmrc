# Ensure optional dependencies are always installed
# This fixes the rollup native module issue: https://github.com/npm/cli/issues/4828
optional=true

# Use legacy peer dependency resolution to avoid conflicts
legacy-peer-deps=true
